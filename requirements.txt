# World Model - Dependencies

# Media acquisition
yt-dlp>=2024.1.0

# Audio/video processing
ffmpeg-python>=0.2.0

# Transcription
openai-whisper>=20231117

# Speaker diarization (optional - requires HuggingFace token)
# Install with: pip install whisperx
# Then accept pyannote model license at: https://huggingface.co/pyannote/speaker-diarization-3.1
whisperx>=3.1.0

# Image processing
opencv-python>=4.9.0
Pillow>=10.0.0

# OCR (for visual text extraction from keyframes)
easyocr>=1.7.0

# Utilities
tqdm>=4.66.0          # Progress bars
python-dateutil>=2.8.0

# NLP / Entity extraction
spacy>=3.7.0
# Post-install: python -m spacy download en_core_web_md

# Real-time display
rich>=13.0.0          # Terminal UI for live streaming

# File synchronization
filelock>=3.12.0      # Cross-platform file locking

# PyTorch (required for whisperx/diarization, also enables GPU Whisper)
torch>=2.0.0
torchaudio>=2.0.0

# Semantic search & topic clustering
sentence-transformers>=2.2.0
scikit-learn>=1.3.0
